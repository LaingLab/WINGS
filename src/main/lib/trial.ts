/** 
Trial Runtime
    1. Start trial called
    2. Initialize devices
    3. Start recording
    4. Event loop
    5. Save data and logs
*/

import { TrialInfo } from '@shared/models'

// Start trial
export const startTrial = (trialInfo: TrialInfo) => {
  console.log('Starting trial... ', trialInfo)
}

// Init devices

// Start recording

// Event loop

// Save data / logs
